<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window is transparent behind the system bars and MainActivity pads
         the WebView by the real insets, so the page lays out exactly as it does
         in a browser while the bars pick up the page's own background colour. -->
    <style name="Theme.Showcase" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/page_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Shown for the moment between tapping the icon and the WebView having
         something to paint, so the app never opens on a blank white window. -->
    <style name="Theme.Showcase.Splash" parent="Theme.Showcase">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>
